import { beforeAll, describe, it } from "vitest";

describe("CreateSellerUseCase", () => {
  it("should throw EntityAlreadyExist if find an existing seller", async () => {});

  it("should create seller", async () => {});
});
